# Custom Rules for GoodSleep Project

- **Git Commit & Push**: Always stage (`git add`), commit (`git commit -m "..."`), and push (`git push`) to GitHub whenever changes are made or a new page is created.
